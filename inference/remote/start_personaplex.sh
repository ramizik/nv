#!/usr/bin/env bash
# Remote NVIDIA GB10: PersonaPlex voice service (STRETCH — not on the demo critical path).
# For the hackathon we default to a transcript fixture. Wire this only after dashboard +
# chat are solid. Fill in the real launch command for your PersonaPlex build here.
set -euo pipefail
echo "[placeholder] PersonaPlex voice service."
echo "Demo uses shared/sample_payloads/*.json transcripts by default — live voice is optional."
exit 0
